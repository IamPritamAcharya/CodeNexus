import 'package:flutter/material.dart';

class CenterTextColors {
  static const Color subtitleColor = Colors.grey;
  static const Color primaryTextColor = Colors.white;
  static List<Color> gradientColors = [
    Colors.pinkAccent, 
    Colors.purple,
    Colors.teal
    ];
}

class NavColors {
  static const Color backgroundGradientStart = Color(0xFF1E88E5);
  static const Color backgroundGradientEnd = Colors.transparent;
  static const Color separatorLine = Color(0xFF90CAF9);
  static const Color navTextColor = Colors.white;
  static const Color navTextColor2 = Color.fromARGB(255, 0, 211, 190);
  static const Color navSignInBorder = Color.fromARGB(255, 129, 66, 245);

  // Glassmorphic and lime theme colors
  static Color limeBackground = Colors.lime.shade600; // Light lime background
  static Color limeAccent = Colors.transparent; // Lime for border
  static const Color limeText = Colors.black; // Dark green text color
}

